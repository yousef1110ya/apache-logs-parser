# this is a dummy data please check it for your system before blaming the code . 
ACCESS_LOG_PATH = (
    "/shared-logs/access.log"
)

ERROR_LOG_PATH = (
    "/shared-logs/error.log"
)

# and this port is not right as well , so you might need to check that out. 
PROMETHEUS_PORT = 8000
